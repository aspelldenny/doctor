# Discovery Report — P006: `doctor lane-check` anchor counter scope fix

**Date:** 2026-05-28
**Phiếu:** P006-lane-check-scope-fix.md (V2, CHALLENGE Turn 1 + Architect RESPOND: [O1.1] ACCEPT test count wording fix)
**Status:** Shipped

---

## Task 0 Anchor Verification Results

| # | Anchor | Result |
|---|--------|--------|
| 1 | `src/cli/lane_check.rs` L58-59: regex `r"(?m)^\|\s*\d+\s*\|"` inline in `execute()`, full document | ✅ Confirmed — L58-59 `anchor_re` + `find_iter(&content).count()`, full-document scope, exactly the bug |
| 2 | Counter inline in `execute()`, no existing helper; `count_constraints()` L119 as precedent pattern | ✅ Confirmed — L26 `pub fn execute`, L103 `pub fn run` (thin wrapper). Counter bare inline at L58-59. `count_constraints()` L119 pattern followed. |
| 3 | 5 tests in `tests/lane_check_test.rs` (not 8 as V1 stated) | ✅ Confirmed (V2 patch applied) — 5 `#[test]` fn at L22/L36/L45/L57/L65. Pattern: `tmp_ticket()` + `doctor()` helper + inline string + NamedTempFile. |
| 4 | `TICKET_TEMPLATE.md` heading structure stable for fuzzy match | ✅ Pre-verified by Architect — `## Task 0 — Verification Anchors` L42, `## Debate Log` L75. Safe. |
| 5 | P001 phiếu: Task 0 (5 rows L46-51) + Debate Log recap table (5 rows L68-73) | ✅ Pre-verified by Architect. Dogfood confirms: post-fix exit 0 "5 anchors". |

---

## Assumptions — CORRECT

- `count_constraints()` precedent pattern (heading-scope via Regex find + slice + next-heading stop) was directly applicable for `count_task0_anchors()`. Zero deviation from phiếu spec.
- Regex `(?mi)^##\s+Task\s+0\b` correctly matches `## Task 0`, `## Task 0 — Verification Anchors`, `## task 0` (case-insensitive). Does NOT match `### Task 0` (subheading) or `## Task 01` (`\b` word boundary).
- Scope stop regex `(?m)^##\s` correctly halts at next `## ` sibling heading; `###` subheadings stay inside scope as intended.
- `cargo build` clean, 0 warnings.

---

## Assumptions — WRONG / Drift Found

None. All phiếu assumptions confirmed correct against code reality.

---

## SOUND Oracle

Claim: anchor counter only counts rows in `## Task 0` section
Oracle: `cargo test` (3 new test cases: scope-fix fixture, no-task0 fixture, P001 real file regression) + manual dogfood
Soundness: SOUND — heading regex + scope slice is exact mechanical operation, no judgment involved

---

## Dogfood Evidence

**P001 live dogfood (bug originally filed):**
```
$ ./target/debug/doctor lane-check --ticket docs/ticket/P001-lane-check.md
OK (Normal — 235 lines, 5 anchors, 5 constraints)
exit: 0
```
Pre-fix: `Normal lane: 10 anchors > 5 cap` → exit 1. Post-fix: exit 0. Bug resolved.

**P006 self-eat:**
```
$ ./target/debug/doctor lane-check --ticket docs/ticket/P006-lane-check-scope-fix.md
Normal lane: 294 lines > 250 cap
exit: 1
```
Exit 1 because phiếu is 294 lines > 250 cap (Normal lane). Correct behavior — phiếu is long due to Debate Log + detailed task spec. Anchor count = 5 (Task 0 only, Debate Log rows excluded correctly). The 294-line trigger is a real violation, not a false positive.

---

## Tests

- 5 pre-existing lane_check tests: all pass (regression clean).
- 3 new tests added:
  1. `ok_normal_task0_scoped_ignores_debate_log_table` — 4 Task 0 rows + 4 Debate Log rows = 8 raw matches; scoped counter returns 4 → exit 0.
  2. `ok_no_task0_heading_treats_as_zero` — no `## Task 0` heading; numbered rows elsewhere; returns 0 → exit 0.
  3. `ok_p001_real_phieu_passes` — P001 phiếu real file integration regression; post-fix exit 0.
- Full suite: 32 tests pass (8 lane_check + 2 mcp + 7 rotate + 8 runtime + 7 validate).

---

## Edge Cases Verified

1. **No `## Task 0` heading** — returns 0 cleanly, no error. SOUND only.
2. **`### Task 0` subheading** — regex `(?mi)^##\s+Task\s+0\b` requires `^##\s+` (exactly 2 hashes + space), so `###` does NOT match. Safe.
3. **`## Task 01`** — `\b` word boundary stops match. Safe.
4. **`## Task 0` is last heading (no next `##`)** — `scope_end = after_heading.len()` (EOF fallback). Correct.
5. **`## Task 0` with no table rows** — `row_re.find_iter(scope).count()` returns 0. Correct.
6. **`### Pre-phiếu snapshot` subheading inside Task 0** — stays inside scope (stop only at `^##\s` level 2). Correct per TICKET_TEMPLATE L55.

---

## Scope Expansions

None. Implementation exactly within edit_allow:
- `src/cli/lane_check.rs` — Task 1: extract `count_task0_anchors()`, swap caller at L58-59.
- `tests/lane_check_test.rs` — Task 2-3: 3 new test cases (2 required + 1 optional P001 regression).
- `CHANGELOG.md` — Fixed section entry added.
- `docs/discoveries/P001.md` — 1-line pointer note added per phiếu Docs Gate.
- `docs/discoveries/P006.md` — this file.

BACKLOG.md had pre-existing uncommitted changes from orchestrator (P006 entry added). Worker did NOT modify BACKLOG.md — outside edit_allow, untouched.

---

## Tier Escalation

None. Phiếu was Tầng 2 throughout. No schema/API/auth/dep triggers fired. LOC delta: ~25 lines `lane_check.rs` (new function) + ~40 lines `lane_check_test.rs` (3 new tests) = 65 LOC total, within ≤ 100 LOC constraint.
