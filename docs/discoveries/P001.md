# Discovery Report — P001: `doctor lane-check`

**Date:** 2026-05-28
**Phiếu:** P001-lane-check.md (V1, CHALLENGE accepted, 0 objections)
**Status:** Shipped

---

## Task 0 Anchor Verification Results

| # | Anchor | Result |
|---|--------|--------|
| 1 | `src/cli/lane_check.rs` with `pub fn run(_args: Args)` + `todo!()` | ✅ Confirmed L25-26 |
| 2 | `Args` struct with `ticket: PathBuf` + `#[arg(long)]` | ✅ Confirmed L21-22 |
| 3 | `src/main.rs` already wires `LaneCheck` subcmd | ✅ Confirmed L31, L50 — Task 4 SKIPPED |
| 4 | `clap`, `anyhow`, `regex` in `[dependencies]` | ✅ Confirmed Cargo.toml L15,22,24 |
| 5 | `assert_cmd`, `predicates`, `tempfile` in `[dev-dependencies]` | ✅ Confirmed Cargo.toml L30-31 (tempfile in `[dependencies]` L23) |

Note: `tempfile` lives in `[dependencies]` (not `[dev-dependencies]`) — used by rmcp at runtime. Works fine for test use too.

---

## Assumptions — CORRECT

- `Regex::new(r"(?i)\*\*Lane:\*\*\s*(Normal|Guarded|Fast)")` correctly matches both blockquote header style (`> **Lane:** Normal`) and plain style (`**Lane:** Normal`). The `>` prefix is not anchored in the regex so it doesn't interfere.
- `todo!()` placeholder in `run()` replaced cleanly with no signature changes needed (`_args` renamed to `args` — Tầng 2 per Debate Log Turn 1 note).
- Task 4 (wire subcmd) was correctly SKIPPED — `main.rs` L31/L50 already had full dispatch.
- `cargo build` clean (0 warnings, 0 errors) after implementation.

---

## Assumptions — WRONG / Drift Found

**Anchor row overcounting in phiếu-as-self-fixture (edge case):**

The anchor regex `(?m)^\|\s*\d+\s*\|` correctly counts all numbered table rows across the entire document. The P001 phiếu itself contains two numbered tables: the Task 0 table (5 rows: `| 1 |` through `| 5 |`) AND the Debate Log summary table (also 5 rows with the same format). Total = 10 anchors, exceeding the Normal cap of 5.

Impact: the self-referential boundary test (Case 9, planned as optional) was removed from the test suite. The anchor counter is working per spec — the spec says "anchor rows = numbered rows in Task 0 table" but the regex cannot distinguish Task 0 rows from identically-formatted Debate Log rows without additional context.

Claim: anchor counter only counts Task 0 rows
Oracle: manual inspection of P001 phiếu + test run (`cargo test --test lane_check_test`)
Soundness: PARTIAL — regex is SOUND for counting, but "Task 0 rows only" requires heading-scoped parsing not currently implemented.

This is a Tầng 2 limitation: behavior is documented, no user-visible harm (Debate Log rows are not present in normal/fresh phiếu files before CHALLENGE). Worker notes this for future phiếu P002+ if a "strict Task 0 only" version is needed.

---

## Exit Code Pattern Chosen

Used `std::process::exit(1)` / `std::process::exit(2)` directly in the `run()` function (not propagating via `anyhow::Error`). Rationale: exit codes are part of the CLI contract (BACKLOG spec), not Rust error semantics. Using `process::exit` makes the exit code explicit and grep-able vs encoding into error variants. Precedent: advisory-inbox uses the same pattern for sub-check failures.

---

## Scope Expansions

None. Implementation stayed exactly within edit_allow:
- `src/cli/lane_check.rs` — Task 1-3 (parse/count/gate)
- `tests/lane_check_test.rs` — 8 test cases (4 required + 4 optional)
- `CHANGELOG.md` — created per phiếu step 7
- `docs/discoveries/P001.md` — this file

`src/main.rs` NOT touched (anchor #3 was ✅).

---

## Edge Cases Found

1. **Blockquote header format** (`> **Lane:** Normal (...)`) — regex `(?i)\*\*Lane:\*\*\s*(Normal|Guarded|Fast)` correctly ignores the `> ` prefix and any trailing text in parentheses. Tested via fixture inline string in test Cases 1-8.

2. **Constraint heading variant** — `count_constraints()` looks for `## Luật chơi` (with Vietnamese characters). If phiếu uses a different heading variant (e.g., `## Constraints` or `## Luật Chơi` with capital C), constraints = 0. This is documented behavior — phiếu template uses exactly `## Luật chơi` per TICKET_TEMPLATE.

3. **Missing `## Luật chơi` heading** — returns 0 constraints cleanly (no error). Correct: phiếu may legitimately have no constraints section.

4. **Debate Log numbered tables** — as noted above, adds to anchor count. For real phiếu files in active/pending state (before CHALLENGE), this is not an issue.

---

## SOUND Oracle (phiếu requirement)

Claim: lane-check accurately counts and gates per §1 budget
Oracle: `cargo test --test lane_check_test` (8 cases including Normal/Fast/Guarded boundary tests)
Soundness: SOUND — counting lines/anchors/constraints is exact mechanical operation, no judgment involved

---

## Tier Escalation

None. Phiếu was Tầng 2 throughout. No schema/API/auth/dep triggers fired.

---

## Docs Updated

- `docs/DISCOVERIES.md` — index entry appended (see below)
- `CHANGELOG.md` — created with P001 entry
- `docs/ARCHITECTURE.md` — Worker verified L29-41 comments match implementation. No drift found; no edits needed.
